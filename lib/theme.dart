import 'package:flutter/material.dart';

// ═══════════════════════════════════════════════════════════════════════
// Palette — GitHub's own dark ("dimmed") colors so the app's chrome (the
// update banner, the settings sheet) blends seamlessly into the WebView.
// ═══════════════════════════════════════════════════════════════════════

const Color kBg = Color(0xFF0D1117); // canvas
const Color kCard = Color(0xFF161B22); // surface
const Color kInset = Color(0xFF21262D);
const Color kBorder = Color(0xFF30363D);
const Color kInk = Color(0xFFC9D1D9); // primary text
const Color kMuted = Color(0xFF8B949E); // secondary text
const Color kAccent = Color(0xFF2F81F7); // GitHub blue
const Color kWarn = Color(0xFFF85149); // GitHub red
